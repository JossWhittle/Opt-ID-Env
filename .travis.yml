language: python

python:
  - "3.8"

services:
  - docker

before_install:
  - sudo apt-get update

install:
  - docker pull josswhittle/opt-id:env-v3

script:
  - docker run --rm -it -v $(pwd)/tests/:/tmp/tests/ josswhittle/opt-id:env-v3 python -m pytest /tmp/tests/*

